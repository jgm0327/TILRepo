# 2월 6일 TIL

* ### 알고리즘

    * [1726. Tuple with Same Product](https://leetcode.com/problems/tuple-with-same-product/description/)
    
      <details>
      <summary><strong>코드 - 구현(33분)</strong></summary>

        ```java

            class Solution {
                public int tupleSameProduct(int[] nums) {
                    Map<Integer, Integer> count = new HashMap<>();

                    for(int i=0 ; i<nums.length ; i++){
                        for(int j=i+1 ; j<nums.length ; j++){
                            int mul = nums[i] * nums[j];

                            count.put(mul, count.getOrDefault(mul, 0) + 1);
                        }
                    }

                    int answer = 0;
                    for(int key : count.keySet()){
                        if(count.get(key) <= 1)
                            continue;

                        int value = count.get(key);
                        int comp = (value * (value - 1)) / 2;

                        answer += comp * 8;
                    }

                    return answer;
                }
            }

        ```

    </details>
